import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';

export default defineConfig({
	base: 'https://toastargumentative.github.io/ToastsWeb/',
	plugins: [sveltekit()]
});
